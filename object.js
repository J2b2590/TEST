// arrays 

const arr = [1, 2, 3];

arr.length

console.log(arr[0]);

arr.push(1) // an item to the end of the array
arr.pop() // remove something from the end of the array
arr.unshift('something') //puts item at the beginning 
arr.shift() //removes from the beginning of array
arr.reverse() //flip the array around
+=
a = a + 1
-=
a = a - 1
/=;
a = a / 1
*=
a = a * 1

// {} object

const turtle = {
	name: 'marvin',
	age: 102,
	hungry: true
};

const bird = {};

bird.name = 'dee';

// make two objects with 5 properties 
//acces each property in the console

//first object you'll make like the first example

//then like the second example

const rabbit = {
	name: 'rodger',
	age: 25,
	clothes: 'red overalls',
	jailtime: '10yrs',
	girlfriend: 'jessica'
};

const guitar = {};



















